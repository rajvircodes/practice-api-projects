Steps to follow:

1. initialized project (npm init -y)
2. create professional folder structure (MVC)
3. load .env file (
   mongo_uri, port, jwt secret
   ) => server.js(entry point)
4. create basic server => app.js and export to server.js
5. connect database => config/db.js
6. create error handler middleware

7. create models: 
    - User
    - Movie
    - Review
8. implement Authentication + Register + login user password hash 
